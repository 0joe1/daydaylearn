一直以来，0xB8000~0xBFFF这段物理地址空间，是留给显卡的
Intel 的处理器不允许将一个立即数传送到段寄器

每个字符对应着显存中的两个连续字节，前一个是字符的ASCII代码，后一个是显示属性
显示属性：KRGB IRGB 前4位背景色，后四位前景色

数据段（Data Segment，简称DS）的基地址没有固定的默认值。它的初始值需要由程序员手动设置。

偏移地址是以字节为单位的，一个字节为8位
